# Estate Baseline — 2026-08-09

## GitHub

The MIRRORNODE organization contains multiple active product, runtime, governance, experimental, and historical repositories. This baseline intentionally records only a core subset in `manifests/estate.json`; broader classification should proceed without deleting unclassified repositories.

## Vercel

Observed team: **Mirrornode**

Observed projects: **25**

The project list contains clear production candidates alongside likely historical, duplicate, generated, and experimental surfaces. Names alone are insufficient for deletion or consolidation. Each surface must be reconciled against its source repository, production domain, and recent deployment state.

## Supabase

Observed project: **Mirrornode OS**

Observed health: **active / healthy**

Observed migration history: **7 migrations**

Observed public tables: **6**

Current GitHub production deployment disposition: **OFF**

`mirrornode-platform` is the strongest observed runtime consumer of Supabase, but no repository currently contains a verified canonical Supabase CLI migration directory. Migration ownership therefore remains unresolved.

## Next reconciliation pass

1. Map every Vercel project to GitHub repository and domain.
2. Classify production, preview/staging, historical, duplicate candidate, experimental, or unclassified.
3. Identify all repositories that consume Supabase.
4. Recover the current production migration SQL into a reviewed canonical source.
5. Replay migrations against non-production infrastructure.
6. Only after successful replay, configure GitHub/Supabase integration and reconsider production deployment.

## Safety note

This public baseline intentionally excludes credentials, project references, service IDs, private URLs, and detailed security findings.
